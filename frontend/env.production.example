# Production Environment Variables for React Frontend
# Create a .env.production file in the frontend directory with these variables
# These will be used when you run: npm run build

# API Base URL - Update with your production backend URL
REACT_APP_API_URL=https://api.yourdomain.site/api

# Optional: Other environment variables
# REACT_APP_ENV=production

