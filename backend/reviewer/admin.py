from django.contrib import admin
from .models import Reviewer

# Note: Reviewer is registered in core/admin.py with the custom admin site
# Keeping this file for reference, but it won't be used since core/admin.py registers it
