from django.contrib import admin
from .models import Note, Notebook

# Note: These are registered in core/admin.py with the custom admin site
# Keeping these here for reference, but they won't be used since core/admin.py registers them
